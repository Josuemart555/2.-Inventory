import { SortingCategoryPipe } from './sorting-category.pipe';

describe('SortingCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SortingCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
